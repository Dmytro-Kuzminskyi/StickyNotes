<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/revert_add_note_activity"
        android:title="@string/revert"
        android:icon="@drawable/ic_revert_disabled"
        android:enabled="false"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/confirm_add_note_activity"
        android:title="@string/confirm"
        android:icon="@drawable/ic_confirm_disabled"
        android:enabled="false"
        app:showAsAction="ifRoom"/>
</menu>